[package]
name = "newtua-testutil"
version = "0.1.0"
description = "Internal test helpers (reference `unar` oracle) for the newtua-formats crates."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
